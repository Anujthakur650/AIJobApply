# Required for NextAuth session signing. Use `openssl rand -base64 32` to generate a value in production.
NEXTAUTH_SECRET=insecure-development-secret

# URL of the deployed application. Needed for NextAuth when running behind a custom domain.
NEXTAUTH_URL=http://localhost:3000

# Optional: enable Google OAuth once the credentials are added.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Placeholder for future database integration.
DATABASE_URL=

# Public configuration
NEXT_PUBLIC_APP_NAME=AIJobApply Preview
